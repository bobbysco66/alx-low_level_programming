#include "main.h"
#include <stdio.h>
/**
 *print_square - Entry point
 *@size: number of times
 * Return: nothing
 */
void print_square(int size)
{
int w;
int h;
if (size > 0)
{
for (h = 0; h < size; h++)
{
for (w = 0; w < size; w++)
{
_putchar('#');
}
_putchar('\n');
}
}
_putchar('\n');
}
