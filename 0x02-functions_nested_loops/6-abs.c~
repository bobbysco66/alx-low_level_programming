#include "main.h"
#include <stdio.h>
/**
 *_abs - Entry point
 *@int:  is the int that will use for the argument of the function
 * Return: Always 0 (Success)
 */
int _abs(int)
{
if (int >= 0)
{return (int);
}
else
{return (int * -1);
}
}
