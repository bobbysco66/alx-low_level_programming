#include "main.h"
#include <stdio.h>
/**
 *add - Entry point
 *@a:  is the int that will use for the argument of the function
 * @b: second number
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
return (a + b);
}
