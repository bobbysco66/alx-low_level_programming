#include "main.h"
#include <stdio.h>
/**
 *puts2 - Entry point
 *@s:  is the int that will use for the argument of the function
 * Return: Always 0 (Success)
 */
void puts2(char *str)
{
int c = 0;
int len;
while (s[c] != '\0')
{
c++;
}
len = c - 1;
c = 0;
while (c < len)
{
_putchar(s[c + 1])
c++
}
_putchar('\n');
}
